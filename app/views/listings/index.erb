<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>


    <h2 class="title">All listings</h2>

    <h3> <%= render 'errors' %>
    </h3>
    <br>

    <div>
      <%= form_tag(listings_path, method: :get) do %>
       <br>
       <%= text_field_tag :state, nil, placeholder: "City, State (NY, CA, etc.)", class: "search_bar" %>  <%=submit_tag 'Search', :class => "btn" %>
      <% end %>
    </div>

    <div class="listings_container">
    <% @listings.each do |listing| %>
      <div class="listing_card">
        <div class="listing_card_img">
          <%= image_tag((listing.image_url(:thumb).to_s), :class => "card_img") %>
        </div>
        <div class="listing_card_name">
          <%= link_to listing.name, listing %>
        </div>
      </div>
    <% end %>
    </div>

  </body>
</html>
